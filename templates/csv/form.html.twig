{% extends "base.html.twig" %}

{% block body %}

    <div class="row text-center">
        <h1>Importer de nouvelles stats</h1>
    </div>
    {{ form_start(form) }}
        <fieldset>
            <div class="form-group">
                {{ form_row(form.file) }}
            </div>
            <div class="form-group" >
                {{ form_row(form.Envoyer) }}
            </div>
        </fieldset>
    {{ form_end(form) }}

{% endblock %}