{% extends "base.html.twig" %}

{% block body %}

    <table class="table table-striped">
        <thead>
        <tr>
            <th scope="col">Libellé</th>
            <th scope="col">Suffixe</th>
        </tr>
        </thead>
        <tbody>
        {% for type in types %}
        <tr>
            <th scope="row">{{ type.libelle }}</th>
            {% if not type.suffixe %}
                <td><input Type="text" placeholder="Null"></td>
            {% else %}
                <td><input Type="text" placeholder="{{ type.suffixe }}"></td>
            {% endif %}
        </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}